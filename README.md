# apbd-cw2-git-s33197

Podział klas polega na osobym namespace dla modeli (Models/) i dla serwisów (Services/).

Zdecydowałem się zastosować dziedziczenie dla sprzędu, w celu wydzielenia wspólnych danych, a rodzaje użytkowników rozróżniam polem Enum.

Logika biznesowa znajduje się serwisie, a modele pozostają anemiczną reprezentacją danych.
